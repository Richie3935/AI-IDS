# ============================================================
# AI-Powered IDS — Python Dependencies (Version 7)
# ============================================================
# Install with:
#   pip install -r requirements.txt
#
# On Linux you may also need:
#   sudo apt-get install python3-dev libpcap-dev   # Debian/Ubuntu
#   sudo dnf install python3-devel libpcap-devel   # Fedora/RHEL
#
# On macOS:
#   brew install libpcap   # usually pre-installed via Xcode CLTools
#
# On Windows:
#   Install Npcap from https://npcap.com/ (WinPcap-compatible mode)
# ============================================================

# Core packet capture library
scapy>=2.5.0

# MySQL alert logging
mysql-connector-python>=8.0

# Flask dashboard
flask>=3.0

# Machine-learning pipeline (Version 7)
scikit-learn>=1.4
numpy>=1.26
pandas>=2.2
joblib>=1.3

# Development / testing
# pytest>=8.0
# pytest-cov>=5.0
# flake8>=7.0
# black>=24.0